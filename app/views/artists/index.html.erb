<h1>All Artists</h1>
<% @artists.each do |artist| %>
    <h3><%= link_to artist.name, artist %></h3>
    <%= artist.song_count > 1 ? "#{artist.song_count} songs" : "#{artist.song_count} song" %>
<% end %>