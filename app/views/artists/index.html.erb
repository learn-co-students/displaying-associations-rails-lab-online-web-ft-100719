<h1>All Artists</h1>

<ul>
    <% @artists.each do |a| %>
        <li><%= link_to a.name, artist_path(a) %> - <%= pluralize(a.song_count, "song") %></li>
    <% end %>
</ul>
<hr>
<%= link_to "Songs", songs_path %> | <%= link_to "NEW ARTIST", new_artist_path %>