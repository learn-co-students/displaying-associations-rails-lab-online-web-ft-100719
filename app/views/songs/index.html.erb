<h1>All Songs</h1>

<ul>
    <% @songs.each do |s| %>
        <%= link_to (s.artist_name + " - " + s.title), song_path(s) %>
    <% end %>
</ul>
<hr>
<%= link_to "Artists", artists_path %> | 
<%= link_to "NEW SONG", new_song_path %>